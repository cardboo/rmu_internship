# migrations/ contains SQL files. They're run by the admin via
# phpMyAdmin / mysql CLI, never reached over HTTP.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
